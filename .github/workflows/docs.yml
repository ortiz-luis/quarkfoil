name: Documentation

on:
  push:
    branches: [main]
    paths:
      - app/**
      - docs/**
      - example/**
      - src/**
      - tests/static_export_ci.py
      - THIRD_PARTY_LICENSES.md
      - mkdocs.yml
      - pyproject.toml
      - uv.lock
  pull_request:
    paths:
      - app/**
      - docs/**
      - example/**
      - src/**
      - tests/static_export_ci.py
      - THIRD_PARTY_LICENSES.md
      - mkdocs.yml
      - pyproject.toml
      - uv.lock
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Install uv
        uses: astral-sh/setup-uv@c771a70e6277c0a99b617c7a806ffedaca235ff9 # v9.0.0
        with:
          enable-cache: true
      - name: Install documentation dependencies
        run: uv sync --frozen --group docs --group browser
      - name: Build documentation
        run: uv run --frozen --no-sync mkdocs build --strict
      - name: Export live example
        run: uv run --frozen --no-sync quarkfoil export example/deck.md --output site/example --cdn
      - name: Test live example export
        run: uv run --frozen --no-sync python tests/static_export_ci.py site/example --browser firefox
      - name: Configure GitHub Pages
        if: github.event_name != 'pull_request' && github.repository == 'juanjosegarciaripoll/quarkfoil'
        uses: actions/configure-pages@45bfe0192ca1faeb007ade9deae92b16b8254a0d # v6.0.0
      - name: Upload documentation
        if: github.event_name != 'pull_request' && github.repository == 'juanjosegarciaripoll/quarkfoil'
        uses: actions/upload-pages-artifact@fc324d3547104276b827a68afc52ff2a11cc49c9 # v5.0.0
        with:
          path: site

  deploy:
    if: github.event_name != 'pull_request' && github.repository == 'juanjosegarciaripoll/quarkfoil'
    needs: build
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Publish to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@cd2ce8fcbc39b97be8ca5fce6e763baed58fa128 # v5.0.0
